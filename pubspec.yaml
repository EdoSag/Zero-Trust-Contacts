name: zerotrust_contacts
description: A new Nowa project.
publish_to: none
version: 1.0.0+1
environment:
  sdk: ">=3.2.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  nowa_runtime: ^0.1.3
  provider: ^6.1.5
  shared_preferences: ^2.5.3
  dio: ^5.5.0+1
  flutter_dotenv: ^5.1.0
  flutter_secure_storage: ^9.2.2
  sqflite: ^2.3.3
  sqlcipher_flutter_libs: ^0.6.4
  cryptography: ^2.7.0
  local_auth: ^2.3.0
  flutter_bloc: ^8.1.6
  path_provider: ^2.1.4
  path: ^1.9.0
  encrypt: ^5.0.3
  go_router: ^17.0.1
  supabase_flutter: ^2.9.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  assets:
    - .env
